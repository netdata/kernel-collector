---
name: CI
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  build:
    name: Build
    strategy:
      matrix:
        kernel_version:
          - '5.4.18'
          - '4.19.102'
          - '4.14.170'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Prepare Build Image
        run: |
          docker build --build-arg KERNEL_VERSION=${{ matrix.kernel_version }} -t kernel-collector .
      - name: Build It
        run: |
          docker run --rm -v $PWD:/kernel-collector kernel-collector
